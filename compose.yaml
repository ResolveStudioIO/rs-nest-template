services:
    db:
        image: postgres:16
        env_file: .env
        ports:
            - '5432:5432'
        healthcheck:
            test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-user} -d ${POSTGRES_DB:-mydatabase}']
            interval: 5s
            timeout: 5s
            retries: 10

    api:
        build: .
        env_file: .env
        environment:
            DATABASE_URL: postgresql://${POSTGRES_USER:-user}:${POSTGRES_PASSWORD:-password}@db:5432/${POSTGRES_DB:-mydatabase}
            NODE_ENV: development
        depends_on:
            db:
                condition: service_healthy
        ports:
            - '3000:3000'
